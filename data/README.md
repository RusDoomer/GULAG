each language gets its own subdirectory containing a file which has every character in that language, a subdirectory for corpora and a subdirectory for layouts

weights have their own subdirectory here
